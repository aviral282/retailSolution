import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.test.retailsolution',
  appName: 'retail-solutions',
  webDir: 'dist/retailSolutions',
  bundledWebRuntime: false
};

export default config;
